format_version: "11"
default_step_lib_source: "https://github.com/bitrise-io/bitrise-steplib.git"

workflows:
  primary:
    steps:
    - git-clone: {}
    - android-build:
        inputs:
        - project_location: "."
        - module: "app"
        - variant: "debug"
    - deploy-to-bitrise-io: {}
